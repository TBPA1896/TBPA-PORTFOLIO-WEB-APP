{
  "_comment": "Do not commit legacy builds/routes — Nitro writes Vercel Build Output API to .vercel/output. In Vercel → Settings → Build & Deployment set Output Directory to .vercel/output (not dist or dist/client)."
}
